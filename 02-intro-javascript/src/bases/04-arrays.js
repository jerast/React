

// Arreglos en JS
// const arreglo = new Array( 100 ); 

const arreglo = [1,2,3,4,4];
// console.table( arreglo ); 

// const arreglo2 = [ ...arreglo, 5 ]; 
// const arreglo3 = [ arreglo, 5 ].flat();
// const arreglo4 = arreglo3.map( item => item );

// arreglo.map( item => item * 2 );
// arreglo.filter( item => item > 2 );
// arreglo.every( item => item > 2 );
// arreglo.some( item => item > 2 );
// arreglo.find( item => item == 4 );
// arreglo.findIndex( item => item == 4 );
// arreglo.at(-5);
// arreglo.toString();
// arreglo.slice(0,3);
// arreglo.length();

// arreglo.reverse();
// arreglo.unshift(3);
// arreglo.push(5);
// arreglo.shift();
// arreglo.pop();

console.table(arreglo);
